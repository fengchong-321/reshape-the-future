#!/bin/bash
# 统计日志总行数
# 用 wc 命令统计文件中有多少条访问记录。
wc -l access.log

# 提取所有状态码为 200 的记录
# 使用 grep 找出所有成功（状态码 200）的请求行。
greo ' 200$' access.log

# 统计每个状态码出现的次数
# 从日志中提取状态码，并统计每种状态码的数量，按次数降序排列。

# 列出访问过 /index.html 的 IP 地址（去重）
# 找出所有请求路径为 /index.html 的 IP，去除重复，每行一个。

# 统计每个 IP 的访问次数，并显示访问次数最多的前 3 个 IP
# 统计每个 IP 出现的次数，按次数排序，取前 3 名。

# 找出所有状态码为 404 的请求，并输出“IP 路径”格式（如 192.168.1.3 /about）
# 只输出 IP 和请求路径，用空格分隔。

# 统计 GET 请求和 POST 请求各自的数量
# 分别统计请求方法为 GET 和 POST 的请求数。

# 找出所有在 10:15:30 到 10:15:40 之间（包含）的访问记录
# 使用 awk 或 sed 根据时间字段筛选。

# 将日志中的 IP 地址替换为匿名形式（保留前两段，如 192.168.x.x）
# 用 sed 将每个 IP 的最后两段替换为 x.x。

# 统计每个路径（如 /index.html）的访问次数，按次数降序显示
# 提取请求路径，统计每个路径的访问次数。